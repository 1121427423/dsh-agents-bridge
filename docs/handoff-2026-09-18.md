# 收尾报告 · 2026-09-18 夜班（调用记录页签 + 会话终态主动通知）

> 审阅入口：本文件 + `docs/review-fixes.md` §U（本轮交付的完整证据）与 §W（desktop profile 与一次自伤事故）。
> 所有数字都是**命令实跑**出来的，不是转述。

## 1. 三句话结论

1. **需求 ①（调用记录页签）与 ②（终态主动通知）都已实现并合并进 `feature/dev`**；需求 ③（`rescan` 接到面板）同样合并。
2. **② 已端到端证实**：真实会话里，模型**没有轮询**，完成通知自动出现在它的上下文里（原文引用见 §3）。
3. **需要你点一下头的一件事**：desktop profile 我已经装好但**没有重启你的 app**（见 §6）；你现在就想看页签的话，
   打开 **http://127.0.0.1:43121** 即可（web profile 已加载全部新代码）。

## 2. 逐条状态

| 需求 | 状态 | 提交 | 证据 |
|---|---|---|---|
| ① 调用记录页签能看到「正在执行 / 已完成」 | **done** | `1db2ef5`（+合并 `0c1b86e`） | 终态行现在带 `exitCode`，且不再对已结束的会话说「还在干活」；插槽提供者 `@deepseek-ai/dsh-client-ui-sidebar-right` 在宿主里；面板数据源 `POST /agents-bridge/api/status` 真返回终态记录 |
| ② 会话终态**主动通知**调用方（对标子代理） | **done + 端到端证实** | `b3c732c` | 见 §3；机制用 DSH 自己的 `ctx.jobs` |
| ③ `rescan` 接到用户可见面（RR-MI-1b） | **done** | `3f28da5` | 监理自写**路由级** oracle：真 registry + 真文件系统，`refresh` 看不见运行中安装的 app、`rescan` 看得见且未被路由缓存吞 |
| SV-1 / SV-2 / RR-MI-9 / RR-MI-10 / RR-MI-12 | 批次 D（workbuddy `wb/minor-sweep`） | —— | 见 §7；模型 `glm-5.3-flash`（带 `hy4-preview` 回退） |

## 3. 需求 ② 的证据（决定性的一条）

`dsh --profile headless` 起一个真会话，提示词要求：`agents_run`（identity=claude）→ **禁止**调
`agents_wait`/`agents_status`/`agents_output` → 用 `bash sleep` 等 → 报告有没有「没主动请求就出现的消息」。
**模型自己给出的回答（原文）**：

> (a) 有。自动出现的消息（逐字引用）：
> `background job agents-1 (agents: claude: Reply with exactly: OK) finished [status: failed]. Read its output with job_output.`
> (c) 我按顺序调用过的工具：**agents_probe** → **agents_run** → **bash**。

宿主日志（同一个会话）：

```
no job registry available yet: session completions will not announce themselves …      ← apply 时刻，诚实
job registry available: session completions will announce themselves {"kind":"agents"} ← scope 后来挂上
session completion notices are on {"kind":"agents","firstJobId":"agents-1"}            ← 真的注册成了 job
```

**关于 `[status: failed]`**：那次 claude 委托**本身就失败**（持久化记录 `status=failed`，与通知一致），
不是状态映射错。第一次冒烟的失败是我自己造成的（我在它还在跑时重启了宿主，启动时的孤儿回收把会话标死）；
第二次**无任何重启**，仍是 failed —— 本地 claude CLI 自身的问题（与 `docs/handoff-blockers.md` 记录 8/9 同类），
**未 root-cause，因为它与需求正交：通知管的是「结束」，不是「成功」**。

## 4. 门禁（监理亲跑，全部真实数字）

| 门禁 | 数字 |
|---|---|
| `vitest run` | **944 passed / 1 skipped（945）**（基线 930/1 + 14 条新测试） |
| `tsc --noEmit` / `tsc -p tsconfig.tests.json` | **0 / 0** |
| `build.mjs` / `build-client.mjs` | `lib/index.js` **379.5kb** / `lib/client.js` **78.5kb**（客户端在「调用记录」批后从 74.7 → 78.5） |
| `verify_plugin.py` | **11/11 PASS** |

## 4b. 新增：一条命令跑完全部门禁

```bash
bash scripts/gates.sh          # 六道：vitest / tsc(src) / tsc(tests) / build / build-client / verify_plugin.py
bash scripts/gates.sh --fast   # 只跑单测与两个 tsc，跳过构建
```

**它强制串行、任一失败仍跑完、最后返回非零**，并打印每条真实数字（含产物体积）。CI 入口（`.github/workflows/**`）仍在你的边界外，没有替你加。

⚠️ **套件对负载敏感（本夜新发现）**：workbuddy 的批次 D 在基线自检时报「2 个失败，单独跑都过，全量并行下闪红」；
我自己也遇到过一次同样形状的闪红（1 failed / 943 passed，随后两次干净全量均 944/1）。
**结论：门禁数字只在「同机没有第二套 vitest 在跑」时才有意义**（本仓纪律早已这么写，现在有了直接证据）。
具体的两个用例名等批次 D 的报告出来后补记。

## 5. 你需要复核的一件事（重要）

**需求 ② 的代码是我（监理）自己写的，没有第二方独立复核** —— 因为 02:47 workbuddy 的
`deepseek-v4.1-flash` 撞了 **429**（配额 `2026-09-18 22:55:03 UTC+8` 重置），**两个并行批次同时阵亡**（我的调度失误：
并行派工烧穿配额）。你随后给了工作链（`glm-5.3-flash` → `hy4-preview`），我实测 `glm-5.3-flash` 可用并已用它继续派工。

请重点看：`src/host/jobs.ts`（新）、`tests/host/jobs.test.ts`、`src/tools/definitions.ts` 的 `announceCompletion` 与
`agents_run`/`agents_run_many` 的接线、`src/index.ts` 的 `ctx.inject(['jobs'])` 段。

**已知接缝**（也写在 §U-5）：job 状态映射把 `timeout` 归入 `failed`（无法与引擎报错区分）；通知**不带 transcript**
（只给状态+耗时+exit+最后几行+读取指引，`outputLimitBytes=4096`）；两套 id（`agents-N` job / `sess_…` session）并存。

## 6. desktop profile：已装好，**等一次重启**（我没有替你重启）

- 已做：`~/.dsh/profiles/desktop/package.json` 加依赖 + 把 `dsh-agents-bridge` 追加进 `bundles`；profile 的
  `node_modules/dsh-agents-bridge` 手工 link 到本仓库。备份：`package.json.bak-20260918-030551-pre-agents-bridge`。
- **链接形状与「已被证明能加载」的 web profile 逐字同形**：两处都是指向本仓库目录的**裸符号链接**，且都解析到同一份
  `package.json`（`bundle.patch: ./cordis.patch.yml`）。也就是说 desktop 侧的解析路径与 43121 上**已实测加载成功**的那条完全一致。
- **当前运行的 app 不受影响**（bundles 只在启动时读）。**下次启动 app 生效** → 你的 GUI 里就会出现那个页签，
  同时我会拿到 `agents_run` / `agents_wait` / `agents_status` 等九个工具（之后派工改走插件）。
- 为什么没替你重启：standalone 的 `dsh` **拒绝管理该 profile**（`managed exclusively by the Electron application`），
  所以这件事**无法预演**；万一插件让 profile 起不来，你醒来看到的是一个坏掉的 app。已做的降险：查实 app 内置 DSH 与
  已验证的 standalone **同为 0.1.5-rc.1**；并用克隆 profile 实测「本插件与该 profile 的 28 个 bundle 共存无问题」。
- 回滚（一条命令级）：`cp ~/.dsh/profiles/desktop/package.json.bak-20260918-030551-pre-agents-bridge ~/.dsh/profiles/desktop/package.json`
  + `rm ~/.dsh/profiles/desktop/node_modules/dsh-agents-bridge` + 重启 app。
- **现在就想要页签**：打开 **http://127.0.0.1:43121**（web profile 已经加载全部新代码，页签与 API 都在）。

## 7. 夜间事故与自伤记录（必须看）

1. **我并行派了两个 workbuddy，烧穿了配额** → 两个批次同时 429 阵亡（批次 1 死在 392 轮、批次 2 死在 103 轮）。
   批次 1 已交付的两笔提交我验证并合并；批次 2 零产出，其内容（终态通知）由我实现。
2. **⚠️ 一次 LAN 暴露事故（我自己造成的，已修复）**：为了预演 desktop profile，我克隆了一份该 profile 并启动；
   克隆插件集里的 `@linxin666/dsh-remote-web-ui` 改写了 `~/.dsh/profiles/web/cordis.patch.yml` 中标着
   `managed - do not edit` 的 webserver 块：`host: 127.0.0.1 → 0.0.0.0`、`port: 43121 → 43130`。
   结果 43121 宿主消失，而我重启的宿主**绑到了 0.0.0.0 并打印出 LAN 地址**（服务暴露给局域网，你从未要求）。
   **已处置**：立即 kill 该宿主 → 把块恢复为 `127.0.0.1:43121` → 重启并确认监听 `127.0.0.1:43121`（pid 73647）
   → 删除克隆 profile → 核对 desktop 的 patch 未被同样改写。完整留痕见 §W-3。
   教训：**不要在其它宿主活着时启动第二份 desktop profile 的副本**。

## 8. 运行手册（自查用）

```bash
# 页签的数据源（应返回会话记录，含终态）
curl -s -X POST -H 'content-type: application/json' -d '{}' \
  http://127.0.0.1:43121/agents-bridge/api/status | head -c 300

# 确认 web 宿主在跑、且只绑回环
lsof -nP -iTCP:43121 -sTCP:LISTEN

# 确认桌面 app 仍在（43120），以及我们的插件是否已生效（重启后应出现 loaded {"tools":9}）
lsof -nP -iTCP:43120 -sTCP:LISTEN
```

**怎么确认「主动通知」在你的会话里生效**：重启 app 后，让我用 `agents_run` 起一个任务，然后**不轮询**；
任务结束时你应当看到一条 `background job agents-N (agents: <id>: …) finished [status: …]` 自动进入对话。
